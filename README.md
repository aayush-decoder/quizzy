## Set up this project for first time

### Run these commands in a new dir
`python -m venv .venv`  
`.venv/scripts/activate`  
`pip install -r requirements.txt`  
  
`git clone https://github.com/aayush-decoder/wpp-quiz-project.git quiz`  
`cd quiz`  
`python manage.py runserver`  